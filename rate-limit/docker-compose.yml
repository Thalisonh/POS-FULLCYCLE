version: '3'

services:
  redis:
    image: redis:latest
    restart: always
    ports:
      - "6379:6379"
    networks: 
      - network1
    
  limit:
    container_name: limit
    build: 
      context: .
    # environment:
    #   - REDIS_HOST=redis
    #   - REDIS_PORT=6379
    #   - RATE_LIMIT_IP=5 
    #   - RATE_LIMIT_TOKEN=5
    #   - BLOCK_TIME=5
    ports:
      - "8080:8080"
    depends_on:
      - redis
    networks: 
      - network1
      
networks:
  network1: